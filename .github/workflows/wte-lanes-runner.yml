name: WTE lanes public runner
on:
  push:
    branches: [wte-lanes-runner]
  pull_request:
    branches: [main]
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: echo WTE_PUBLIC_RUNNER_OK
